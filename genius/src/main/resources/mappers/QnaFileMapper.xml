<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/schema/mybatis-3-mapper.dtd">
<mapper namespace="org.fullstack4.genius.mapper.QnaFileMapper">
    <select id="getFileList" resultType="org.fullstack4.genius.domain.QnaFileVO">
        SELECT file_idx, qna_idx, save_name, original_name, path
        FROM tbl_qna_file
        WHERE qna_idx = #{qna_idx}
    </select>
    <insert id="regist">
        INSERT INTO tbl_qna_file(qna_idx, save_name, original_name, path)
        VALUES(#{qna_idx}, #{save_name}, #{original_name}, #{path})
    </insert>
    <select id="getFile" resultType="org.fullstack4.genius.domain.QnaFileVO">
        SELECT file_idx, qna_idx, save_name, original_name, path
        FROM tbl_qna_file
        WHERE file_idx = #{file_idx}
    </select>

</mapper>